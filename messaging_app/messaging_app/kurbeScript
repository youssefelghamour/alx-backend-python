#!/bin/bash
# A script to set up kubernetes

# Check if minikube is installed
if command -v minikube &> /dev/null; then
    echo "Minikube found. Starting a Kubernetes cluster..."
    minikube start --driver=docker  # Start a local Kubernetes cluster using Docker

    echo "Cluster started. Showing cluster info..."
    kubectl cluster-info  # Verify that the cluster is running

    echo "Retrieving all pods in all namespaces..."
    kubectl get pods -A  # Retrieve the available pods
else
    echo "Minikube is not installed"
    exit 1
fi
